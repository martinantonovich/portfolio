---
import { getCollection } from 'astro:content';
import ExperienceItem from './ExperienceItem.astro';

// Para agregar una experiencia nueva: copiá un archivo de src/content/experience/*.md
// y completá el frontmatter. Se ordena automáticamente por fecha de inicio (más reciente primero).
const allExperience = await getCollection('experience');
const experiences = allExperience.sort((a, b) => b.data.startDate.localeCompare(a.data.startDate));
---

<section id="experiencia" class="mx-auto max-w-3xl px-6 py-20">
  <div data-reveal class="mb-12 text-center">
    <h2 class="text-3xl font-bold tracking-tight sm:text-4xl">Experiencia</h2>
    <p class="mt-3 text-zinc-600 dark:text-zinc-400">Mi recorrido profesional hasta ahora.</p>
  </div>

  <div>
    {
      experiences.map((experience, index) => (
        <ExperienceItem experience={experience} isLast={index === experiences.length - 1} />
      ))
    }
  </div>
</section>
